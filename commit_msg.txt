Add entrypoint for WSL2 CUDA driver detection

- entrypoint.sh: detects and preloads WSL2 CUDA driver at startup
- Falls back to CPU silently if no WSL2 GPU found
- docker-compose.yml: restore deploy.devices GPU reservation syntax
- Note: PyTorch CUDA init fails on Docker Desktop WSL2 due to known
  driver shim incompatibility; nvidia-smi works but torch.cuda does not
